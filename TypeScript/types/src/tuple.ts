let aluno2 : {
    nome: string,
    aprovado?: boolean,
    idade: number,
    notas: [number, number, number, number]
}

aluno = {
    nome: "Mateus",
    // aprovado: true,
    idade: 21,
    notas: [1, 2, 3, 4]
}